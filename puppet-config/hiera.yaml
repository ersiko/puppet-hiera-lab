---
:backends:
  - yaml
:yaml:
  :datadir: /etc/puppet/data
:hierarchy:
  - "hosts/%{fqdn}"
  - "env/%{env}/role/%{role}"
  - "role/%{role}"
  - "datacenter/%{datacenter}"
  - "env/%{env}"
  - common
